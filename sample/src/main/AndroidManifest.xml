<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zlc.season.sample">

    <application
        android:name=".CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UriActivity" />
        <activity android:name=".UriForResultActivity" />
        <activity android:name=".ActionActivity">
            <intent-filter>
                <action android:name="zlc.season.sample.action" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ActionForResultActivity">
            <intent-filter>
                <action android:name="zlc.season.sample.action_for_result" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ClassActivity" />
        <activity android:name=".ClassForResultActivity" />
        <activity android:name=".UriMenuActivity" />
        <activity android:name=".ActionMenuActivity" />
        <activity android:name=".ClazzMenuActivity"></activity>
    </application>

</manifest>